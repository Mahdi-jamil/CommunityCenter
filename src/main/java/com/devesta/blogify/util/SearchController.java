package com.devesta.blogify.util;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1/search")
public class SearchController {

    /*
    * /posts/{name} title contains name
    * /community/{name} name contains name
    * /comments/{body} body contains name
    * /people/{name} name contains name done maybe
    * */
}
